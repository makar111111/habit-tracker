# Те, без чого застосунок не працює.
# Встановити:  pip install -r requirements.txt

# --- Бекенд ---
fastapi[standard]==0.141.1
sqlmodel==0.0.42

# Читання файлу .env. Фізично воно вже приїхало разом із fastapi[standard],
# але ми імпортуємо його у своєму коді напряму — тож і в списку має бути
# явно. Покладатись на чужі залежності не можна: у наступній версії
# fastapi цей пакет може зникнути, і проєкт зламається без причини.
python-dotenv==1.2.3

# --- Telegram-бот ---
aiogram==3.31.0

# Асинхронний HTTP-клієнт. Ним бот звертається до власного ж API.
# Раніше стояв лише в requirements-dev.txt (його використовував TestClient),
# тепер це повноцінна залежність застосунку — тому переїхав сюди.
httpx==0.28.1
