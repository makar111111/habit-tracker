# Те, що потрібно лише для розробки й тестів.
# Встановити:  pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.1.1

# Дозволяє писати тести для async-функцій: без нього pytest побачив би
# корутину, нічого з нею не зробив і зарахував би тест як пройдений.
pytest-asyncio==1.4.0
