[pytest]
# auto означає: pytest-asyncio сам розпізнає async-тести й async-фікстури.
# Без цього рядка кожен async-тест довелося б позначати вручну,
# а забутий тест мовчки зараховувався б як пройдений — найгірший
# різновид помилки в тестах: червоне, що прикидається зеленим.
asyncio_mode = auto
